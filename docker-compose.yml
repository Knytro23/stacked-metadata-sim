services:
  metadata-simulator:
    build: .
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      METADATA_SIM_MAX_FILES: 50
      METADATA_SIM_MAX_UPLOAD_MB: 512
