name: Build — Windows & macOS

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install Pillow piexif fonttools pyinstaller

      - name: Rebuild PyInstaller bootloader (breaks AV signature)
        run: |
          git clone --depth 1 https://github.com/pyinstaller/pyinstaller.git _pyi_src
          cd _pyi_src\bootloader
          python waf all --target-arch=64bit
          cd ..
          pip install .
        shell: cmd

      - name: Build Windows .exe
        run: pyinstaller metadata_simulator.spec

      - name: Rename for platform
        run: mv dist/MetaDataSimulator.exe dist/MetaDataSimulator_Windows.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MetaDataSimulator_windows
          path: dist/MetaDataSimulator_Windows.exe

      - name: Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/MetaDataSimulator_Windows.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install Pillow piexif pyinstaller fonttools

      - name: Build macOS .app
        run: pyinstaller metadata_simulator.spec

      - name: Zip macOS app
        run: cd dist && zip -r MetaDataSimulator_macOS.zip MetaDataSimulator.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: MetaDataSimulator_macOS
          path: dist/MetaDataSimulator_macOS.zip

      - name: Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/MetaDataSimulator_macOS.zip
